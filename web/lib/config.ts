/**
 * Deliberately empty.
 *
 * This file used to hold `ORG`, `ROLES`, `TIER_POOL_MBPS`, `TIER_VLAN` and `ENFORCEMENT` — an
 * invented VLAN per tier, an invented bandwidth pool per tier, a fabricated cache age, and a
 * developer's hostname, all rendered in the console as if they were telemetry. The organization
 * lives in `lib/ens/config.ts`; groups and their tiers come from the enforcer; branches come
 * from ENS. Nothing here had a source, so nothing here survived.
 */
export {};
